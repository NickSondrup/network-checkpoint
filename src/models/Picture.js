export class Picture {
  constructor(pictureData) {
    this.title = pictureData.titles
    this.linkUrl = pictureData.linkUrl
    this.tall = pictureData.tall
    this.square = pictureData.square
    this.banner = pictureData.banner
  }
}
